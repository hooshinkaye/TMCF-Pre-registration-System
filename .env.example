# PostgreSQL Database Connection (from Render)
DATABASE_URL=postgresql://username:password@host:port/dbname

# Or use individual vars (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tmcf_enrollment
DB_USER=postgres
DB_PASS=

# Server Configuration
NODE_ENV=production
PORT=5000
